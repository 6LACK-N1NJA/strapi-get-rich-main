'use strict';

/**
 * comparison-uniq-text router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::comparison-uniq-text.comparison-uniq-text');
