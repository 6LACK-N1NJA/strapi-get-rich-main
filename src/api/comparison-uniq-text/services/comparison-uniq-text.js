'use strict';

/**
 * comparison-uniq-text service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::comparison-uniq-text.comparison-uniq-text');
