'use strict';

/**
 * comparison-filters-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::comparison-filters-config.comparison-filters-config');
