'use strict';

/**
 * comparison-filters-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::comparison-filters-config.comparison-filters-config');
